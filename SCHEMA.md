# Registry schema

The catalog uses `schemaVersion: 1`. Each plugin entry contains an npm package `name`, published semver `version`, source `repository` URL, optional source `path`, and a human-readable `description`. Entries must point to packages in the `@pi-harness/plugin-*` namespace.
