# Updating the catalog

Only add a plugin after its npm publication succeeds. Keep entries deterministic and review package name, version, repository, and compatibility before merging. The registry is metadata only; plugin source belongs in `pi-harness-plugins`.
