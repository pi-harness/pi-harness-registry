{
  "name": "@pi-harness/registry",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "npm run validate:schema && node validate.mjs",
    "sync:npm": "node scripts/sync-npm.mjs",
    "validate:schema": "ajv validate -s schema.json -d plugins.json --spec=draft2020"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pi-harness/pi-harness-registry.git"
  },
  "overrides": {
    "fast-json-patch": "3.1.1"
  }
}
