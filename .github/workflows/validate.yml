name: Validate registry
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
on:
  push:
  pull_request:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v5
        with: {node-version: 22}
      - run: npx --yes ajv-cli@5 validate -s schema.json -d plugins.json --spec=draft2020
      - run: node validate.mjs # "const x=require('./plugins.json'); if(x.schemaVersion!==1||!Array.isArray(x.plugins)) process.exit(1); console.log('validated '+x.plugins.length+' plugins')"
