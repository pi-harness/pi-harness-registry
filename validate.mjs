import { readFileSync } from 'node:fs'; const x=JSON.parse(readFileSync('plugins.json')); const n=new Set(); if(x.schemaVersion!==1||!x.plugins.length)throw Error('invalid catalog'); for(const p of x.plugins){if(n.has(p.name)||!/^@pi-harness\/plugin-/.test(p.name)||!/^[0-9]+\.[0-9]+\.[0-9]+/.test(p.version))throw Error(`invalid entry ${p.name}`);n.add(p.name);new URL(p.repository)} console.log(`validated ${n.size} catalog entries`);
